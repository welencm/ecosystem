---
- name: "Install RedHat useful packages"
  yum:
    name: "{{ item }}"
    state: present
    update_cache: yes
  with_items:
    - openssh
    - openssh-clients
    - openssh-server
  become: true

- name: "Disable selinux"
  selinux:
    state: disabled
  become: true
